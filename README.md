# QRKot — Благотворительный фонд поддержки котиков

## Описание проекта

Вымышленный благотворительный фонд поддержки котиков **QRKot** собирает пожертвования на всевозможные задачи, связанные с помощью кошачьей популяции: на корм, на медицинское обслуживание, организацию приютов и на другую помощь котам, кошкам и котятам.

Приложение позволяет:
- Создавать целевые проекты с конкретной суммой сбора
- Отправлять пожертвования в фонд
- Автоматически распределять пожертвования по проектам в порядке их создания

## Технологии

- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0** — ORM для работы с БД
- **Pydantic** — валидация данных
- **SQLite + aiosqlite** — база данных
- **Alembic** — миграции БД
- **uvicorn** — ASGI сервер

## Установка и запуск

1. Создайте виртуальное окружение:
```bash
python -m venv venv
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Примените миграции:
```bash
alembic upgrade head
```

4. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

## API документация

После запуска сервера документация Swagger доступна по адресу:
- http://localhost:8000/docs

## Структура проекта

```
app/
├── api/
│   └── endpoints/       # Эндпоинты API
├── core/                # Конфигурация и БД
├── models/              # SQLAlchemy модели
├── schemas/             # Pydantic схемы
└── services/            # Бизнес-логика (инвестирование)
```
